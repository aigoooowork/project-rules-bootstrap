<!-- TEMPLATE METADATA — renderer MUST remove this comment and prepend the selected registry entry's exact adapter ID, support, scope, and loading metadata. This shared body is identity-neutral. -->
# Rule navigation

Explicitly reference `.ai/rules/project.md`, then only applicable domain files. This adapter adds no project rules.
